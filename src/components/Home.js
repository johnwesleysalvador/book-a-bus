import React, {PureComponent} from 'react'

import {Header} from './common'

class Home extends PureComponent {
    render() {
        return (
            <Header headerText="Book that bus" />
        )
    }
}

export default Home